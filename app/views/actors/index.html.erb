<% if flash[:error]%>
    <div id="errors">
        <%= flash[:error] %>
    </div>
<% end %>


<div class="index">
    <ul>
    <% @actors.each do |actor|%>
        <li><%= link_to  actor.name, actor_path(actor) %></li>
    <% end %>
    </ul>
</div>