<h1>Casting Directors</h1>
<% if !creative? %>
    <ul>
    <% @catsing_directors.each do |cd|%>
        <li><%= link_to  cd.name, catsing_director_path(cd) %>
        <% if current_user.casting_directors.include?(cd) %>
            *
        <% end %>
        </li>
    <% end %>
    </ul>
    <p>* Indicates a Casting Director for a Project in your tracked auditions</p>
<% else %>
    <%@casting_directors.each do |cd|%>
        <ul>
            <li><%= link_to  cd.name, casting_director_path(cd) %></li>
        </ul>
    <% end %>
<% end %>